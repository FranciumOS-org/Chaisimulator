{
  "name": "mypixel",
  "version": "1.0.0",
  "private": true,
  "description": "مای پیکسل — فروشگاه اشیای کلکسیونی (این فایل فقط دستورها را به backend منتقل می‌کند)",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "seed": "npm --prefix backend run seed",
    "reset": "npm --prefix backend run reset",
    "dedupe": "npm --prefix backend run dedupe"
  }
}
